# Security and caching headers (used by Netlify / Cloudflare Pages when deploying from dist)
# GitHub Pages does not apply these; use a reverse proxy or CDN to add headers if needed.

/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()

# Cache static assets
/assets/*
  Cache-Control: public, max-age=31536000, immutable
